# ============================================
# CONFIGURAZIONE POGGIOFACE CON INSIGHTFACE
# ============================================

# === INSIGHTFACE API SERVICE ===
# Host e porta del servizio InsightFace (docker-compose)
HOST=http://localhost
PORT=8000

# API Key (opzionale - per compatibilità con vecchie configurazioni)
# InsightFace non richiede API key ma la accetta per retrocompatibilità
API_KEY=your-api-key-here

# Plugin per il riconoscimento (age, gender) - opzionale
FACE_PLUGINS=age,gender

# === DASHBOARD ===
# Password per accesso alla dashboard
DASHBOARD_PASSWORD=your-secure-password

# Chiave segreta per sessioni Flask
SECRET_KEY=your-secret-key-change-in-production

# URL del servizio PoggioFace (per comunicazione Dashboard -> PoggioFace)
POGGIO_FACE_URL=http://localhost:5002

# === POGGIOFACE ===
# URL della Dashboard (per comunicazione PoggioFace -> Dashboard)
DASHBOARD_URL=http://localhost:5000

# === DISPOSITIVI ESTERNI ===
# URL del relay Shelly (opzionale)
SHELLY_URL=http://192.168.1.100/relay/0?turn=on

# ============================================
# NOTE PER LA MIGRAZIONE DA COMPREFACE
# ============================================
# 
# 1. HOST e PORT ora puntano al servizio InsightFace
#    (prima: CompreFace su porta 8000)
#    
# 2. API_KEY è mantenuta per retrocompatibilità ma non è richiesta
#    da InsightFace
#
# 3. SIMILARITY_THRESHOLD e DETECTION_THRESHOLD sono ora gestiti
#    nel file docker-compose.yml del servizio InsightFace:
#    - SIMILARITY_THRESHOLD=0.6 (più alto = più restrittivo)
#    - DETECTION_THRESHOLD=0.5
#
# 4. Per avviare il servizio InsightFace:
#    cd insightface_service
#    docker-compose up -d
#
