# ============================================
# CONFIGURAZIONE CENTRALIZZATA POGGIOFACE
# ============================================
# Questo file è letto da: PoggioFace, Dashboard, InsightFace Docker
# ============================================

# === INSIGHTFACE API SERVICE ===
# Host e porta del servizio InsightFace (docker-compose)
HOST=http://localhost
PORT=8000

# API Key (opzionale - per compatibilità con vecchie configurazioni)
API_KEY=your-api-key-here

# === SOGLIE DI RICONOSCIMENTO (0.0 - 1.0) ===
# SIMILARITY_THRESHOLD: soglia per considerare un match valido (più alto = più restrittivo)
# DETECTION_THRESHOLD: soglia minima di probabilità per considerare un volto rilevato valido
SIMILARITY_THRESHOLD=0.5
DETECTION_THRESHOLD=0.5

# Plugin per il riconoscimento (age, gender) - opzionale
FACE_PLUGINS=age,gender

# === DASHBOARD ===
# Password per accesso alla dashboard
DASHBOARD_PASSWORD=your-secure-password

# Chiave segreta per sessioni Flask
SECRET_KEY=your-secret-key-change-in-production

# URL del servizio PoggioFace (per comunicazione Dashboard -> PoggioFace)
POGGIO_FACE_URL=http://localhost:5002

# === POGGIOFACE ===
# URL della Dashboard (per comunicazione PoggioFace -> Dashboard)
DASHBOARD_URL=http://localhost:5000

# === DISPOSITIVI ESTERNI ===
# URL del relay Shelly (lasciare vuoto se non usato)
SHELLY_URL=

# ============================================
# NOTE IMPORTANTI
# ============================================
# 
# 1. Questo file .env è la UNICA fonte di configurazione per tutti i componenti:
#    - PoggioFace (client)
#    - Dashboard (server)
#    - InsightFace Docker (motore riconoscimento)
#
# 2. Dopo aver modificato SIMILARITY_THRESHOLD o DETECTION_THRESHOLD:
#    - Riavviare il container Docker: docker-compose down && docker-compose up -d
#    - Riavviare PoggioFace e Dashboard
#
# 3. Per avviare il servizio InsightFace:
#    cd insightface_service
#    docker-compose up -d
